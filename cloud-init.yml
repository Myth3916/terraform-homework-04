#cloud-config

users:
  - name: admin
    groups: sudo
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ${ssh_public_key}

packages:
  - nginx

runcmd:
  - systemctl enable nginx
  - systemctl start nginx